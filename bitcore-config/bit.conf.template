# Vorlage für Bit Core (B1T). Das Image überschreibt /data/bit.conf mit Env-Variablen.
# Diese Datei sorgt nur dafür, dass das Image die vollständige Config (inkl. Indizes) erzeugt.
# RPC_USER, RPC_PASSWORD, NETWORK kommen aus docker-compose.yml.
